{
  "name": "life-ai",
  "version": "1.0.0",
  "description": "Personal life management app — pantry tracking, expiry alerts, recipe suggestions",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
